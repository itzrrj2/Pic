# Basic dependencies for Telegram bot and image processing
pyTelegramBotAPI==4.12.0
requests==2.28.1
Pillow==9.2.0
torch==1.13.1+cpu  # Use this for CPU, for GPU use `torch==1.13.1+cu116`
realesrgan==0.1.0

# Optional: For advanced handling of model weights (if you need to download large models)
wget==3.2
